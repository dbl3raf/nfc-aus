# Normalise line endings so a regenerated export does not show up as a
# whole-file diff. The export scripts run on Windows and write CRLF;
# everything is stored in the repository as LF.
* text=auto eol=lf
*.wav binary
*.png binary
*.jpg binary
*.mp3 binary
